erver:
  port: 8888

spring: 
  cloud:
    config:
      server:
      # Pour un repo public
        git:
          uri: https://github.com/mohamedutopios/server-config
          clone-on-start: true
          default-label: master